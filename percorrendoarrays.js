let numeros = [1, 2, 3, 4, 5];
let nomes = ['Yemuri', 'Ícaro', 'Carlos', 'Luana', 'Laura'];
for (let i = 0; i < numeros.length; i++) {
    console.log(`Número: ${numeros[i]} - Pessoa: ${nomes[i]}`);
}